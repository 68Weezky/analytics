<nav class="bg-blue-800 text-white shadow-md">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center">
      <!-- Mobile menu button -->
      <div class="md:hidden">
        <button type="button" class="text-white hover:text-blue-200 focus:outline-none" aria-controls="mobile-menu" aria-expanded="false">
          <span class="sr-only">Open main menu</span>
          <i class="fas fa-bars text-xl"></i>
        </button>
      </div>

      <!-- Navigation Links -->
      <div class="hidden md:flex space-x-4" id="desktop-menu">
        <a href="/standings" class="nav-link <%= title === 'standings' ? 'text-blue-200 font-bold' : 'text-white hover:text-blue-200' %> px-3 py-4">
          <i class="fas fa-table mr-2"></i>Standings
        </a>
        <a href="/teams" class="nav-link <%= title === 'teams' ? 'text-blue-200 font-bold' : 'text-white hover:text-blue-200' %> px-3 py-4">
          <i class="fas fa-users mr-2"></i>Teams
        </a>
        <a href="/matches" class="nav-link <%= title === 'matches' ? 'text-blue-200 font-bold' : 'text-white hover:text-blue-200' %> px-3 py-4">
          <i class="fas fa-calendar-alt mr-2"></i>Matches
        </a>
        <a href="/stats" class="nav-link <%= title === 'stats' ? 'text-blue-200 font-bold' : 'text-white hover:text-blue-200' %> px-3 py-4">
          <i class="fas fa-chart-bar mr-2"></i>Stats
        </a>
      </div>
    </div>

    <!-- Mobile menu -->
    <div class="md:hidden hidden" id="mobile-menu">
      <div class="flex flex-col space-y-2 px-2 pb-3 pt-2">
        <a href="/standings" class="nav-link <%= title === 'standings' ? 'bg-blue-700 text-white' : 'text-white hover:bg-blue-600' %> block px-3 py-2 rounded-md">
          <i class="fas fa-table mr-2"></i>Standings
        </a>
        <a href="/teams" class="nav-link <%= title === 'teams' ? 'bg-blue-700 text-white' : 'text-white hover:bg-blue-600' %> block px-3 py-2 rounded-md">
          <i class="fas fa-users mr-2"></i>Teams
        </a>
        <a href="/matches" class="nav-link <%= title === 'matches' ? 'bg-blue-700 text-white' : 'text-white hover:bg-blue-600' %> block px-3 py-2 rounded-md">
          <i class="fas fa-calendar-alt mr-2"></i>Matches
        </a>
        <a href="/stats" class="nav-link <%= title === 'stats' ? 'bg-blue-700 text-white' : 'text-white hover:bg-blue-600' %> block px-3 py-2 rounded-md">
          <i class="fas fa-chart-bar mr-2"></i>Stats
        </a>
      </div>
    </div>
  </div>
</nav>

<script>
  // Mobile menu toggle functionality
  document.querySelector('[aria-controls="mobile-menu"]').addEventListener('click', function() {
    const menu = document.getElementById('mobile-menu');
    const isExpanded = this.getAttribute('aria-expanded') === 'true';
    
    this.setAttribute('aria-expanded', !isExpanded);
    menu.classList.toggle('hidden');
  });
</script>

<style>
  .nav-link {
    transition: all 0.3s ease;
  }
  .nav-link:hover {
    transform: translateY(-2px);
  }
</style>
